<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="facturacion.android.aplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".scrDatosFactura2"></activity>
        <activity android:name=".scrMisFacturas" />
        <activity
            android:name=".scrLogin"
            android:label="Facturación">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".scrMenu"
            android:label="Búsqueda Ticket" />
        <activity
            android:name=".Registro"
            android:label="Nuevo Usuario" />
        <activity
            android:name=".scrFactura"
            android:label="Datos Ticket" />
        <activity
            android:name=".scrDatosCliente"
            android:label="Mis Datos" />
        <activity
            android:name=".scrDatosFactura"
            android:label="Datos Factura" />
        <activity
            android:name=".MainActivity"
            android:label="Búsqueda Ticket" />
    </application>

</manifest>